# AstroPhysics Module

Supports importing of FITS datasets using the `astropy` Python module. Primary focus is data stemming from the ALMA radio telescope. The `FitsVolumeRaycaster` processor visualizes volumetric data while applying a depth corrections for the positions around a star.
